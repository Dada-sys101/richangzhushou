# 任务执行契约：V15-CTRL-001

契约版本：v2.1.1-final
任务名称：V1.5 唯一总执行规划与治理基线落地
批准日期：2026-08-10
当前状态：`VERIFYING / PR_OPEN`（本地存在未提交治理修订）

## 1. 目标与依赖

将已人工批准的 v2.1.1 Final 规划、Accepted ADR-026 和 docs/40 V1.1 落地为无冲突治理基线。
Base 为 `codex/v15-integration-foundation@bc747b7...`，任务分支为
`codex/v15-ctrl-001-rebaseline`，Draft PR 为 #10。PR #8/#9 已合入是启动前置。

## 2. 允许与禁止范围

允许修改 `PLANS.md`、execution-state、ADR-026、docs/40、本任务契约、必要的 context/session/
status/index/README/decisions/changelog 派生说明，以及状态模型要求的最小 context 校验脚本。

禁止修改业务代码、`apps/`、`packages/`、Prisma、migration、依赖、正式 CI、云资源、环境、
Staging 和 Production。未经各自授权不得 commit、push、更新 PR、merge 或开始 PR6a。

## 3. 已批准决策

- AI 为 R1，Push 为 R1.1，新 RRULE/Import 为 R2，完整 IndexedDB 迁移/Shrink 为 R3；
- REL-01 可提前设计但不得创建资源；REL-02 等待 R1 Quality Gate 和独立授权；
- PR18 包含完整 Proposal 确认 UI；PR20 development dependency 与 H7 人工 merge gate 分离；
- AI-DECISION-001 使用 provisional effect thresholds 和不可降低安全阈值；
- Production 必须经过 integration RC→main PR→CI/Review/授权→main HEAD→tag；
- docs/40 V1.1 保留核心技术架构，由 Accepted ADR-026 增量修订发布和门禁映射；
- Task Selection Policy 当前固定 `currentTask: V15-CTRL-001`，完成后下一任务为 `PR6a`。

## 4. 执行与验证

1. 核验 GitHub/Git/CI 实时事实和干净目标工作树；
2. 仅在允许范围落地批准文本；
3. 运行 `npm run check:context` 和 `git diff --check`；
4. 审查完整 diff、敏感信息和超范围文件；
5. 停止，等待 commit 授权。

## 5. 十二项完成条件

1. v2.1.1 Final 人工批准；
2. ADR-026 人工批准并 Accepted；
3. REL-01 依赖调整批准；
4. REL-02 Quality Gate 调整批准；
5. main/tag 发布门禁批准；
6. docs/40 必要章节同步为 V1.1；
7. PLANS、execution-state、ADR、docs/40 无有效冲突；
8. PR #10 最终 diff 经人工批准；
9. PR #10 更新后 CI 成功；
10. 获得独立 merge 授权；
11. 合入 `codex/v15-integration-foundation`；
12. 核验 integration HEAD 与正式规划完全一致。

全部满足前不得标记 `DONE_INTEGRATION`，PR6a 不得开始。人工门禁不得由本任务自动关闭。
